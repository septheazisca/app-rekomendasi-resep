flask
pandas
scikit-learn
pillow
python-dotenv
numpy
requests

# tensorflow dan google-genai dihapus (tidak dipakai di v3)
# inference-sdk dihapus (tidak support Python 3.13)
# requests sudah cukup untuk panggil Roboflow API langsung

# jalankan: pip install -r requirements.txt